---
name: Fix Lint and Test Errors
description: This prompt fixes errors in the provided code so that it passes all linting and test checks.
---

Please update the code so that both `npm run lint` and `npm run test:run` pass successfully, and no errors are reported in VS Code.

If the terminal output cannot be captured, write the output to a file inside the `.tmp` folder and use it for verification.
